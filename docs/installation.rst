============
Installation
============

At the command line::

    $ easy_install pyappstore

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyappstore
    $ pip install pyappstore
